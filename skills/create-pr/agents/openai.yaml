interface:
  display_name: "Create PR"
  short_description: "Args: target branch (default: main). Create a pull request."
  default_prompt: "Specify a target branch, else defaults to 'main'."
policy:
  allow_implicit_invocation: false