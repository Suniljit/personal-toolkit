interface:
  display_name: "Wiki"
  short_description: "Args: ingest | add | query | lint. Create and manage wiki pages"
  default_prompt: "ingest | add | query | lint"
policy:
  allow_implicit_invocation: false
