interface:
  display_name: "Implement"
  short_description: "Args: spec and base branch. Build work from a spec or tickets"
  default_prompt: "Specify a spec to implement and a base branch to perform code review against."
policy:
  allow_implicit_invocation: false
