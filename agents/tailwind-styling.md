# Tailwind and Styling

Scope: component styling.

Avoid hardcoded color values in components (inline `style`, arbitrary Tailwind classes like `bg-[#123456]`, `text-[rgb(...)]`, `border-[color:...]`). Use project color tokens in `global.css`.
